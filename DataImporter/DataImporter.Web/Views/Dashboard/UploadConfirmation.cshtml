@model DataImporter.Web.Models.ImportContactModel
@{
    ViewData["Title"] = "UploadConfirmation";
}

@section Styles
{
    <link rel="stylesheet" href="/admin/plugins/datatables-bs4/css/dataTables.bootstrap4.min.css">
}

@section Scripts
{
    <script src="/admin/plugins/datatables/jquery.dataTables.min.js"></script>
    <script src="/admin/plugins/datatables-bs4/js/dataTables.bootstrap4.min.js"></script>
    <script>
        $(function () {
            $('#example2').DataTable({
                "paging": true,
                "lengthChange": false,
                "searching": false,
                "ordering": true,
                "info": true,
                "autoWidth": false,
                "responsive": true,
            });
        });
    </script>
}
<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">Will You Confirm Uploading?</h3>
                    </div>
                    <!-- /.card-header -->
                    <div class="card-body">
                        <table id="example2" class="table table-bordered table-hover">
                            <thead>
                                <tr>
                                    @foreach (var item in Model.listColumn)
                                    {
                                        <th>@item</th>
                                    }

                                </tr>
                            </thead>
                            <tbody>

                                @for (var i = 0; i < Model.Values.Count; i++)
                                {
                                    <tr>

                                        @for (var k = 0; k < Model.Values[i].Count; k++)
                                        {
                                            <td>@Model.Values[i][k]</td>
                                        }
                                    </tr>

                                }
                        </table>
                    </div>
                    <!-- /.card-body -->
                </div>
                <!-- /.card -->
            </div>
            <!-- /.col -->
        </div>
        <!-- /.row -->
    </div>
    <section>
        <div class="card-footer">
            <button type="submit" class="btn btn-primary">Upload</button>
            <a type="submit" class="btn btn-primary float-right" asp-controller="Dashboard" asp-action="ManageGroup">Cancel</a>
        </div>
    </section>
    <!-- /.container-fluid -->
</section>
<!-- /.content-wrapper -->
